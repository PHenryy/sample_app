import 'package:flutter/material.dart';
import '../widgets/base_app_bar.dart';

class Cart extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: BaseAppBar(), body: Container());
  }
}
